import {
    Form
  } from 'reactstrap';

const SportsForm = ({props}) => {
   
    return (
        <Form className={props.className}>
             { /* add FormElement */ } 
        </Form>
    )
}

export default SportsForm;